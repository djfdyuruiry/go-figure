using Caliburn.Micro;

namespace GoFigure.App.ViewModels.Menu
{
    class SkillMenuViewModel : HelpMenuViewModel
    {
        public SkillMenuViewModel(IEventAggregator eventAggregator) : base(eventAggregator)
        {
        }
    }
}
